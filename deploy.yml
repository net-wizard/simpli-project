---
- hosts: localhost
  tasks:
    - name: Pull-app
      docker_container:
        name: my-app
        image: thoratshubham/portfolio
        state: started
        detach: yes
        ports:
        - "80:5000"
  